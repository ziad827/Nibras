/**
 * Milestone submission, pulse polling, and feedback modals live in projects-core.js.
 * Submission URL validation helpers live in submission-modal.logic.js.
 */
(function (global) {
  global.NibrasProjectsSubmission = { module: 'projects-core.js' };
})(typeof window !== 'undefined' ? window : globalThis);
