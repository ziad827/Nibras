/**
 * Named constants for @nibras/core.
 */

/** Proactively refresh the CLI session token after this many ms since issue. */
export const TOKEN_REFRESH_THRESHOLD_MS = 7 * 24 * 60 * 60 * 1_000; // 7 days
