meta {
  name: Course Sections
  seq: 12
}

vars:pre-request {
  trackingBaseUrl: http://localhost:8080
}

docs {
  Fastify tracking API — course sections and lecture videos.
  Requires a web session token from the platform API (port 4848 / gateway).
  Set `trackingToken` in your Bruno environment.
}
