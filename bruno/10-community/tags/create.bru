meta {
  name: Create Tag
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/tags
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "graph",
    "description": "Questions about graph theory and algorithms"
  }
}
